# Empty means same-origin API calls, recommended when deploying dist under https://plus.keria.cc.cd
VITE_API_BASE=

# For deployments that proxy API calls through a gateway, set the gateway origin/path.
# VITE_API_BASE=https://your-domain.example
